services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    env_file:
      - ./backend/.env
    environment:
      - PORT=8000
      - DATABASE_URL=sqlite:////app/data/chatbot.db
    volumes:
      - backend_data:/app/data

volumes:
  backend_data:
